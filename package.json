{
  "name": "chipdog",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run -w @chipdog/shared build && npm run -w @chipdog/mobile typecheck",
    "web": "npm run -w @chipdog/mobile web"
  },
  "devDependencies": {
    "expo-module-scripts": "^5.0.8"
  }
}
